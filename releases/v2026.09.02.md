# v2026.09.02 - Production Deployment


---

## ☁ APP Server (Cloud Side)
> Deployed: 02-09-2026 14:08:56 IST | Previous: 19-08-2026 16:57 IST

- No notable changes (metadata-only commits)
---

## 🏫 ERP Server (School Side)
> Deployed: 02-09-2026 17:48:53 IST | Previous: 18-08-2026 15:20 IST

- Skip no-op writes in menu_sections/menu_sections_app_dir sync
- Pdated the board_type table to fill only NULL/blank values for board_name, branch_code, and branch_order, while preserving all existing data. Board names are assigned sequentially (One blank, Two blank, etc.), branch codes start from 2001, and branch orders continue from the highest existing value. The sequence resets independently for each database.
- Update board type null values with sequential branch code and order
- Fixed the RDLC report issue in production by updating the deploy-cloudside.yml pipeline to restore the original .rdlc files after precompilation and before creating the ZIP. This prevents report processing errors without requiring any web.config or publish profile changes.
- Fix RDLC files in deployment package while keeping -u flag
- Added the Voucher Group column to the Vouchers Report by renaming and preserving the existing  group_name data, positioning the column next to Head Name/Voucher Type across Daily Wise, Month Wise, and Grid Row Wise views, and protecting it from being removed by custom column filters. Updated the hardcoded column indices in the RowDataBound events to accommodate the new column while maintaining attachment hyperlink rendering and total row color formatting.
- Fix daily attendance clearing bug where the scheduler skipped re-processing classes. Previously, clearing attendance from the UI successfully reset the attendance data but left behind the evaluated flag in the scholar_att_shift_flag table, causing subsequent runs of the scheduler to falsely assume the shift had already been processed. This update adds backend logic in Class1.cs's delete_attendance method to properly clean up the corresponding shift flags for both single and multiple classes, ensuring the scheduler can accurately regenerate attendance when run manually.
- Fix individual exam report showing fail for all students when single criteria is selected
- Handle alphanumeric board_type_id and branch_code values in board_type update
- Fix duplicate note insertion on salary staff note page